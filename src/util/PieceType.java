package util;

public enum PieceType {
	PAWN,
	KNIGHT,
	BISHOP,
	ROOK,
	QUEEN,
	KING;
	
    @Override
    public String toString() {
        return ""; /*TODO ? fare toString per ricostruire la board ? */
    }
}
